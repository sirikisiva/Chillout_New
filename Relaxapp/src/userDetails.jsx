

import React from "react";
import "./userDetails.css";
import { Link } from "react-router-dom";



const UserDetails = ({ ButtonProps }) => {

  return (
    <div className="details">
      {/* {!ButtonProps ? (
        <Link className="link" to="/Music">
          Music
        </Link>
      ) : (
        <img
          className="icons"
          src="./rightArrow.png"
          alt="Right Arrow"
          width={30}
        />
      )}
      {!ButtonProps ? (
        <Link className="link" to="/Meditation">
          Meditation
        </Link>
      ) : (
        <img
          className="icons"
          src="./rightArrow.png"
          alt="Right Arrow"
          width={30}
        />
      )}
      {!ButtonProps ? (
        <Link className="link" to="/Motivation">
          Motivation
        </Link>
      ) : (
        <img
          className="icons"
          src="./rightArrow.png"
          alt="Right Arrow"
          width={30}
        />
      )}
      {!ButtonProps ? (
        <Link className="link" to="/Friend">
          Friend
        </Link>
      ) : (
        <img
          className="icons"
          src="./rightArrow.png"
          alt="Right Arrow"
          width={30}
        />
      )}
      {!ButtonProps ? (
        <Link className="link" to="/Books">
          Books
        </Link>
      ) : (
        <img
          className="icons"
          src="./rightArrow.png"
          alt="Right Arrow"
          width={30}
        />
      )}
      {!ButtonProps ? (
        <Link className="link" to="/Reels">
          Reels
        </Link>
      ) : (
        <img
          className="icons"
          src="./rightArrow.png"
          alt="Right Arrow"
          width={30}
        />
      )} */}
   
    </div>
  );
};

export default UserDetails;
